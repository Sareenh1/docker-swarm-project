version: "3.9"

services:
  web:
    build: ./app
    image: swarm-demo-app:latest
    ports:
      - "80:3000"
    deploy:
      replicas: 3
      update_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: on-failure
